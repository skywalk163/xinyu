函数 用户注册：
  接收 用户名、邮箱、密码。

  如果 长度 用户名 小于 3：
    返回 "错误：用户名太短"。

  如果 长度 邮箱 小于 5：
    返回 "错误：邮箱格式错误"。

  如果 长度 密码 小于 6：
    返回 "错误：密码太短"。

  定义 用户 = {
    "用户名": 用户名,
    "邮箱": 邮箱,
    "注册时间": "2026-05-25"
  }。

  打印 "用户注册成功"。
  打印 用户名。
  返回 用户。

定义 结果 = 用户注册 "张三"、"zhangsan@example.com"、"password123"。
打印 结果。
