# 心语编程语言 - 面向对象编程示例

# ============ 基础类定义示例 ============

# 1. 基本类定义
定义 类 动物：
    定义 名字 = ""。
    定义 年龄 = 0。

    函数 初始化(参数 名字, 参数 年龄)：
        自身.名字 = 名字。
        自身.年龄 = 年龄。
    。

    函数 叫()：
        返回 "动物叫声"。
    。

    函数 介绍()：
        打印 "我是" 自身.名字 "，今年" 自身.年龄 "岁。"。
    。
。

# 2. 创建对象
定义 我的宠物 = 新建 动物("小白", 3)。
我的宠物.介绍()。
打印 "叫声：" 我的宠物.叫()。

# ============ 继承示例 ============

# 3. 继承
定义 类 狗 继承 动物：
    函数 叫()：
        返回 "汪汪"。
    。

    函数 摇尾巴()：
        打印 自身.名字 "正在摇尾巴..."。
    。
。

定义 类 猫 继承 动物：
    函数 叫()：
        返回 "喵喵"。
    。

    函数 抓老鼠()：
        打印 自身.名字 "正在抓老鼠..."。
    。
。

# 4. 使用继承的类
定义 我的狗 = 新建 狗("旺财", 2)。
我的狗.介绍()。
打印 "狗叫：" 我的狗.叫()。
我的狗.摇尾巴()。

定义 我的猫 = 新建 猫("咪咪", 1)。
我的猫.介绍()。
打印 "猫叫：" 我的猫.叫()。
我的猫.抓老鼠()。

# ============ 多态示例 ============

# 5. 多态
定义 动物列表 = 列表(新建 动物("普通动物", 5), 我的狗, 我的猫)。
打印 "\n多态示例："
循环 动物 遍历 动物列表：
    打印 动物.名字 "的叫声：" 动物.叫()。
。

# ============ 静态成员示例 ============

# 6. 静态属性和方法
定义 类 数学工具：
    静态 定义 圆周率 = 3.14159。

    静态 函数 平方(参数 x)：
        返回 x 相乘 x。
    。

    静态 函数 最大值(参数 a, 参数 b)：
        如果 a 大于 b 那么：
            返回 a。
        否则：
            返回 b。
        。
    。
。

打印 "\n静态成员示例："
打印 "圆周率：" 数学工具.圆周率。
打印 "5的平方：" 数学工具.平方(5)。
打印 "10和20的最大值：" 数学工具.最大值(10, 20)。

# ============ 类方法示例 ============

# 7. 类方法
定义 类 工厂：
    类函数 创建实例(参数 值)：
        返回 工厂(值)。
    。

    函数 初始化(参数 值)：
        自身.值 = 值。
    。

    函数 显示()：
        打印 "工厂实例，值：" 自身.值。
    。
。

定义 实例1 = 工厂.创建实例(100)。
定义 实例2 = 工厂.创建实例(200)。

实例1.显示()。
实例2.显示()。

# ============ 接口示例 ============

# 8. 接口定义
定义 接口 可发声：
    函数 发声()。
。

定义 接口 可移动：
    函数 移动()。
。

# 9. 实现接口
定义 类 鸟 实现 可发声, 可移动：
    定义 名字 = ""。

    函数 初始化(参数 名字)：
        自身.名字 = 名字。
    。

    函数 发声()：
        返回 "叽叽喳喳"。
    。

    函数 移动()：
        返回 "飞翔"。
    。

    函数 介绍()：
        打印 "我是" 自身.名字 "，我会" 自身.移动() "，我的声音是" 自身.发声()。
    。
。

定义 类 鱼 实现 可发声, 可移动：
    定义 名字 = ""。

    函数 初始化(参数 名字)：
        自身.名字 = 名字。
    。

    函数 发声()：
        返回 "咕噜咕噜"。
    。

    函数 移动()：
        返回 "游泳"。
    。

    函数 介绍()：
        打印 "我是" 自身.名字 "，我会" 自身.移动() "，我的声音是" 自身.发声()。
    。
。

# 10. 使用接口
定义 我的鸟 = 新建 鸟("小黄"）。
定义 我的鱼 = 新建 鱼("小金"）。

我的鸟.介绍()。
我的鱼.介绍()。

# ============ 复杂示例：银行系统 ============

# 11. 银行账户系统
定义 类 银行账户：
    定义 _余额 = 0。
    定义 账户名 = ""。

    函数 初始化(参数 账户名, 参数 初始余额)：
        自身.账户名 = 账户名。
        自身._余额 = 初始余额。
    。

    函数 存款(参数 金额)：
        自身._余额 = 自身._余额 相加 金额。
        打印 自身.账户名 "存款" 金额 "元，当前余额：" 自身._余额。
    。

    函数 取款(参数 金额)：
        如果 金额 小于等于 自身._余额 那么：
            自身._余额 = 自身._余额 相减 金额。
            打印 自身.账户名 "取款" 金额 "元，当前余额：" 自身._余额。
            返回 真值。
        否则：
            打印 自身.账户名 "取款失败，余额不足！当前余额：" 自身._余额。
            返回 假值。
        。
    。

    函数 查询余额()：
        返回 自身._余额。
    。

    函数 显示信息()：
        打印 "账户：" 自身.账户名 "，余额：" 自身.查询余额() "元"。
    。
。

# 12. 储蓄账户（继承）
定义 类 储蓄账户 继承 银行账户：
    定义 利率 = 0.03。

    函数 初始化(参数 账户名, 参数 初始余额, 参数 利率)：
        父类.初始化(账户名, 初始余额)。
        自身.利率 = 利率。
    。

    函数 计算利息(参数 年数)：
        定义 利息 = 自身.查询余额() 相乘 自身.利率 相乘 年数。
        打印 自身.账户名 "的" 年数 "年利息：" 利息 "元"。
        返回 利息。
    。

    函数 显示信息()：
        父类.显示信息()。
        打印 "利率：" (自身.利率 相乘 100) "%"。
    。
。

# 13. 使用银行系统
打印 "\n银行系统示例："

定义 账户1 = 新建 银行账户("张三", 1000)。
账户1.显示信息()。
账户1.存款(500)。
账户1.取款(200)。
账户1.取款(2000)。  # 应该失败

定义 储蓄账户1 = 新建 储蓄账户("李四", 5000, 0.035)。
储蓄账户1.显示信息()。
储蓄账户1.存款(1000)。
储蓄账户1.计算利息(1)。

# ============ 模块导出示例 ============

# 14. 模块导出
打印 "\n模块导出示例："

定义 函数 创建动物(参数 名字, 参数 年龄)：
    返回 新建 动物(名字, 年龄)。
。

定义 函数 创建狗(参数 名字, 参数 年龄)：
    返回 新建 狗(名字, 年龄)。
。

定义 函数 创建猫(参数 名字, 参数 年龄)：
    返回 新建 猫(名字, 年龄)。
。

# 导出公共接口
导出 动物, 狗, 猫, 银行账户, 储蓄账户。
导出 创建动物, 创建狗, 创建猫。
导出 动物 为 基础动物。

打印 "所有类和函数已导出，可以在其他模块中使用。"

# ============ 总结 ============

打印 "\n=== OOP示例总结 ==="
打印 "1. 类定义和对象创建 ✓"
打印 "2. 继承和方法重写 ✓"
打印 "3. 多态 ✓"
打印 "4. 静态成员和类方法 ✓"
打印 "5. 接口实现 ✓"
打印 "6. 模块导出 ✓"
打印 "7. 复杂系统设计 ✓"

打印 "\n心语OOP系统已成功实现！"
