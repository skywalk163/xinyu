# 心语编程语言 - 实用算法示例

# 冒泡排序
定 冒泡排序 = 函 列表：
  定 n = 列表长度(列表)。
  遍历 i 于 范围(n)：
    遍历 j 于 范围(0， n减i减1)：
      若 列表[j]大于列表[j加1]：
        定 临时 = 列表[j]。
        列表[j] = 列表[j加1]。
        列表[j加1] = 临时。
      。
    。
  。
  返回 列表。
。

定 测试列表 = 【64， 34， 25， 12， 22， 11， 90】。
印 "排序前：" 测试列表。
印 "排序后：" 冒泡排序(测试列表)。

# 二分查找
定 二分查找 = 函 列表， 目标：
  定 左 = 0。
  定 右 = 列表长度(列表)减1。

  当 左小于等于右：
    定 中 = (左加右)除以2。
    若 列表[中]等于目标：
      返回 中。
    否则若 列表[中]小于目标：
      左 = 中加1。
    否则：
      右 = 中减1。
    。
  。
  返回 负1。
。

定 有序列表 = 【1， 3， 5， 7， 9， 11， 13， 15】。
定 目标值 = 7。
定 位置 = 二分查找(有序列表， 目标值)。
印 目标值 "在列表中的位置：" 位置。

# 素数判断
定 是素数 = 函 n：
  若 n小于2：
    返回 假。
  。
  遍历 i 于 范围(2， n)：
    若 n取模i等于0：
      返回 假。
    。
  。
  返回 真。
。

印 "判断素数："。
遍历 num 于 范围(1， 21)：
  若 是素数(num)：
    印 num "是素数"。
  。
。

# 斐波那契数列生成
定 生成斐波那契 = 函 n：
  定 结果 = 【】。
  定 a = 0。
  定 b = 1。
  遍历 i 于 范围(n)：
    结果.添加(a)。
    定 临时 = a。
    a = b。
    b = 临时加b。
  。
  返回 结果。
。

印 "前10个斐波那契数：" 生成斐波那契(10)。

# 最大公约数（欧几里得算法）
定 最大公约数 = 函 a， b：
  当 b不等于0：
    定 临时 = b。
    b = a取模b。
    a = 临时。
  。
  返回 a。
。

印 "48和18的最大公约数：" 最大公约数(48， 18)。

# 字符串反转
定 反转字符串 = 函 s：
  定 结果 = ""。
  定 长度 = 列表长度(s)。
  遍历 i 于 范围(长度减1， 负1， 负1)：
    结果 = 结果加s[i]。
  。
  返回 结果。
。

印 "反转'心语编程'：" 反转字符串("心语编程")。
