# 心语编程语言 - 函数定义示例

# 定义简单函数
定 平方 = 函 x：
  返回 x乘x。
。

印 "5的平方：" 平方(5)。

# 定义多参数函数
定 相加 = 函 a， b：
  返回 a加b。
。

印 "3加4：" 相加(3， 4)。

# 定义无参数函数
定 问好 = 函：
  印 "你好！欢迎使用心语！"。
。

问好()。

# 函数嵌套调用
定 两倍 = 函 x：
  返回 x乘2。
。

印 "两倍(两倍(5))：" 两倍(两倍(5))。

# 递归函数：阶乘
定 阶乘 = 函 n：
  若 n小于等于1：
    返回 1。
  否则：
    返回 n乘阶乘(n减1)。
  。
。

印 "5的阶乘：" 阶乘(5)。
印 "10的阶乘：" 阶乘(10)。

# 递归函数：斐波那契
定 斐波那契 = 函 n：
  若 n小于2：
    返回 n。
  否则：
    返回 斐波那契(n减1)加斐波那契(n减2)。
  。
。

印 "斐波那契(10)：" 斐波那契(10)。
