# 心语编程语言 - 高级特性示例

# 高阶函数
定 应用函数 = 函 函数 值：
    返回 函数 值。
。

定 双倍 = 函 x：
    返回 x乘2。
。

印 "应用双倍函数到5："。
印 应用函数 双倍 5。

# 函数组合
定 组合 = 函 f g x：
    返回 f (g x)。
。

定 加一 = 函 x：
    返回 x加1。
。

印 "组合加一和双倍："。
印 组合 双倍 加一 5。

# 闭包示例
定 创建计数器 = 函 初始值：
    定 计数 = 初始值。
    定 增加 = 函：
        计数 = 计数加1。
        返回 计数。
    。
    返回 增加。
。

定 计数器 = 创建计数器 0。
印 "计数器："。
印 计数器。
印 计数器。
印 计数器。

# 列表操作
定 映射 = 函 列表 函数：
    定 结果 = 【】。
    遍历 元素 于 列表：
        定 新元素 = 函数 元素。
        # 这里需要列表追加操作
    。
    返回 结果。
。

定 过滤 = 函 列表 条件：
    定 结果 = 【】。
    遍历 元素 于 列表：
        若 条件 元素 则：
            # 这里需要列表追加操作
        。
    。
    返回 结果。
。

# 数学函数
定 绝对值 = 函 x：
    若 x小于0 则：
        返回 零减x。
    否则：
        返回 x。
    。
。

印 "-5的绝对值："。
印 绝对值 -5。

定 最大值 = 函 a b：
    若 a大于b 则：
        返回 a。
    否则：
        返回 b。
    。
。

印 "10和20的最大值："。
印 最大值 10 20。

定 最小值 = 函 a b：
    若 a小于b 则：
        返回 a。
    否则：
        返回 b。
    。
。

印 "10和20的最小值："。
印 最小值 10 20。

# 字符串操作
定 连接字符串 = 函 s1 s2：
    返回 s1加s2。
。

印 "连接字符串："。
印 连接字符串 "你好" "世界"。

# 布尔操作
定 异或 = 函 a b：
    返回 (a或b)且(非(a且b))。
。

印 "真异或假："。
印 异或 真 假。
