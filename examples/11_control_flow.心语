# 心语编程语言 - 控制流示例

# 条件语句
定 分数 = 85。

若 分数 大于等于 90 则：
    印 "优秀"。
否则：
    若 分数 大于等于 80 则：
        印 "良好"。
    否则：
        若 分数 大于等于 70 则：
            印 "中等"。
        否则：
            若 分数 大于等于 60 则：
                印 "及格"。
            否则：
                印 "不及格"。
            。
        。
    。
。

# 嵌套条件
定 年龄 = 25。
定 有工作 = 真。

若 年龄 大于等于 18 则：
    若 有工作 则：
        印 "成年人且有工作"。
    否则：
        印 "成年人但无工作"。
    。
否则：
    印 "未成年人"。
。

# 逻辑运算
定 条件1 = 真。
定 条件2 = 假。

若 条件1 且 条件2 则：
    印 "两个条件都为真"。
否则：
    印 "至少一个条件为假"。
。

若 条件1 或 条件2 则：
    印 "至少一个条件为真"。
否则：
    印 "两个条件都为假"。
。
