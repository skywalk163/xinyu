"""
其他内置函数实现

包含：id, hash, repr, ascii, bin, oct, hex, chr, ord, callable, help, eval, exec, compile, globals, locals
"""

# 占位实现，将在任务9中完善
builtin_id = id
builtin_hash = hash
builtin_repr = repr
builtin_ascii = ascii
builtin_bin = bin
builtin_oct = oct
builtin_hex = hex
builtin_chr = chr
builtin_ord = ord
builtin_callable = callable
builtin_help = help
builtin_eval = eval
builtin_exec = exec
builtin_compile = compile
builtin_globals = globals
builtin_locals = locals
