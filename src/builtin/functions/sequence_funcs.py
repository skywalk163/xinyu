"""
序列操作类内置函数实现

包含：len, range, enumerate, zip, map, filter, sorted, reversed, iter, next, all, any, slice
"""

# 占位实现，将在任务8中完善
builtin_len = len
builtin_range = range
builtin_enumerate = enumerate
builtin_zip = zip
builtin_map = map
builtin_filter = filter
builtin_sorted = sorted
builtin_reversed = reversed
builtin_iter = iter
builtin_next = next
builtin_all = all
builtin_any = any
builtin_slice = slice
