"""
数学类内置函数实现

包含：abs, max, min, sum, pow, round, divmod, complex
"""

# 占位实现，将在任务6中完善
builtin_abs = abs
builtin_max = max
builtin_min = min
builtin_sum = sum
builtin_pow = pow
builtin_round = round
builtin_divmod = divmod
builtin_complex = complex
