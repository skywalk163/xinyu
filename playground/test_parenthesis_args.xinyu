 # 测试括号表达式作为函数参数

 # 测试1：简单括号表达式
定义 加 = 函 a b：
    返回 a 相加 b。
。

定义 乘 = 函 a b：
    返回 a 相乘 b。
。

 # 测试括号表达式作为参数
打印 "加(2 + 3, 4) = " 加 (2 相加 3) 4。
打印 "乘(加(2, 3), 5) = " 乘 (加 2 3) 5。

 # 测试2：递归函数调用
定义 阶乘 = 函 n：
    如果 等 n 0 那么：
        返回 1。
    。
    否则：
        返回 n 相乘 阶乘 (减 n 1)。
    。
    。

    打印 "阶乘(5) = " 阶乘 5。

     # 测试3：复杂表达式
    定义 复杂计算 = 函 x y：
        返回 (x 相加 y) 相乘 (x 相减 y)。
    。

    打印 "复杂计算(10, 3) = " 复杂计算 10 3。
    打印 "复杂计算(5 + 2, 3 - 1) = " 复杂计算 (5 相加 2) (3 相减 1)。
