 # 测试心语函数调用语法

 # 测试1：简单函数调用
定义 平方 = 函 x：
    返回 x 相乘 x。
。

打印 "平方(5) = " 平方 5。
打印 "平方(3 + 2) = " 平方 (3 相加 2)。

 # 测试2：递归函数（阶乘）
定义 阶乘 = 函 n：
    如果 等 n 0 那么：
        返回 1。
    。
    否则：
        定义 减一 = 减 n 1。
            返回 n 相乘 阶乘 减一。
        。
        。

        打印 "阶乘(5) = " 阶乘 5。

         # 测试3：汉诺塔递归（纯言风格）
        定义 汉诺塔 = 函 n 源 目标 辅助：
            如果 等 n 1 那么：
                打印 连 "移动盘子从 " 源 " 到 " 目标。
                。
                否则：
                    定义 新n = 减 n 1。
                        汉诺塔 新n 源 辅助 目标。
                        打印 连 "移动盘子从 " 源 " 到 " 目标。
                        汉诺塔 新n 辅助 目标 源。
                        。
                        。

                        打印 "汉诺塔 3 个盘子："。
                        汉诺塔 3 "A" "C" "B"。

                         # 测试4：带括号的表达式作为参数
                        定义 加 = 函 a b：
                            返回 a 相加 b。
                        。

                        打印 "加(2, 3) = " 加 2 3。
                        打印 "加(1 + 2, 3 + 4) = " 加 (1 相加 2) (3 相加 4)。
