# 心语语言中文词法分析器（自举版本）

# 测试词法分析
定义 测试代码 = "定义 x = 5。"。
打印 测试代码。

# 定义变量
定义 x = 5。
定义 y = 10。

# 函数定义
函数 加法：
  参数 a。
  参数 b。
  返回 a 相加 b。

# 函数调用
定义 结果 = 加法 x y。
打印 结果。

# 条件语句
如果 x 大于 3 那么
  打印 "x大于3"。
否则
  打印 "x小于等于3"。

# 循环语句
循环 i 遍历 [1, 2, 3]：
  打印 i。
